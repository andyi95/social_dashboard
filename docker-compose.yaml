version: '3'

services:
  backend:
    build: backend
    volumes:
      - ./backend:/app:cached
    working_dir: /app
    env_file:
      - .env

  frontend:
    build:
      context: frontend
      dockerfile: stage.Dockerfile